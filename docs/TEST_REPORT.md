# Test report

Executed locally on macOS (Darwin 25.5.0), Node 23.11.0, pnpm 9.4.0, PostgreSQL 14 at `127.0.0.1:54329`, on September 10, 2026. These are local results only; no public-testnet run is recorded here.

## Executed commands and actual outcomes

| Command | Result |
|---|---|
| `pnpm typecheck` | Passed, no errors. |
| `pnpm test` (vitest) | 5 files, 48 tests, all passed. PostgreSQL-backed backend tests ran against `TEST_DATABASE_URL` (no skips reported). |
| `pnpm test:e2e` (Playwright, Chromium) | 9 tests, all passed: public discovery/filter/comparison and private boundary, native controls for all six sport properties, narrow-screen (390px) layout with organizer sandbox, CSRF rejection of cross-origin mission creation. Screenshots in `test-results/`. |
| `forge test --root contracts` | 2 suites, 20 tests, all passed, including an invariant suite (~2,000 calls each across create/progress/time/withdraw handlers, 0 reverts, 0 discards). Local Foundry only; not public-testnet evidence. |
| `pnpm build` | Production Next.js build succeeded after the user-facing copy audit. |

## Incident during this session

The long-running `next dev` process left by the previous session hung (accepted connections, never responded), which made all 9 Playwright tests fail by timeout. Killing the stale process and restarting `pnpm dev` restored all 9 passes with no code change. If e2e tests time out uniformly, check for a wedged dev server on port 3000 first.

## Not executed

- No Base Sepolia transaction, deployment, or source verification. `pnpm smoke:testnet` has no real acceptance runs to verify.
- No live OpenAI model call; `OPENAI_API_KEY` is not configured.
- No container build; the local Docker daemon was unavailable.

See docs/COMPLETION.md for the full implemented / not-exercised / not-implemented breakdown.

## Addendum — later the same evening

Correction: the earlier vitest runs reported "48 passed" with `TEST_DATABASE_URL` set; a subsequent accidental overwrite of `.env` with the template blanked that variable, which made the 4 PostgreSQL-backed tests skip. The `.env` was repaired (fresh secrets generated by `scripts/repair-local-env.ts`; a new verifier wallet was minted because the old private key was unrecoverable) and the suite again runs 48/48 with the database tests executing.

Bring-your-own-key changes verified after implementation:
- `pnpm typecheck`, 48/48 vitest (Postgres suite executing), 9/9 Playwright, production build: all passed.
- Live API checks against the dev server: mission creation without a key returns "This deployment requires your own OpenAI API key to run a mission"; an invalid `sk-` key is rejected by real provider validation with "OpenAI rejected this API key". A valid-key end-to-end agent run remains unexercised (no real key configured).
- `/api/health` reports `byok: true` with `RPC_URL` satisfied; remaining `missing`: `MARKET_CONTRACT_ADDRESS`, `MARKET_BYTECODE_HASH` (filled by testnet deployment).
