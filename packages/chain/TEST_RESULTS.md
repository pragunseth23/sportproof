# Executed chain checks

Execution date: 2026-09-10 America/Los_Angeles. No public deployment or public-testnet transactions were performed; credentials/funded role wallets were unavailable.

- `~/.foundry/bin/forge test --root contracts --summary`: 19 unit/fuzz tests passed, plus 1 stateful invariant. Fuzz: 256 cases. Invariant: 128 runs × 64 calls = 8192 calls, zero handler reverts in final run. Tests cover access, exact price/terms, duplicate orders, immutable publication restrictions, reserved/single-use offers, cancellation/pause survival, all deadline boundaries, acceptance, objective rejection/disputes, normal and timeout outcomes, pull withdrawal, reverting recipient isolation, forced surplus, terminal non-revival, and conservation.
- `pnpm exec vitest run packages/chain/policy.test.ts`: 3 passed. Rejects less than two confirmations, malformed config, disconnected RPC report unlock.
- `ANVIL_BIN=$HOME/.foundry/bin/anvil pnpm exec tsx scripts/chain-local-integration.ts`: PASS. Explicit local integration uses Anvil chain ID 84532 but is not Base Sepolia acceptance. Real signed contract deployment/publication/purchase/attestation/acceptance/withdrawal and rejection/refund/withdrawal. Every operation rejects its one-confirmation receipt before an extra sealed block. No access before delivery attestation; wrong buyer denied; wrong runtime bytecode rejected. Prebroadcast persistence callback exercised.

The Solidity test suite is not a formal audit. Public Base Sepolia acceptance, source verification, hosted-provider reorganization behavior, and real funded-wallet/model mission smoke tests remain external checks. Signer call sites must hold durable wallet locks and atomically reserve application budgets; the chain adapter enforces role/function/value/gas/chain/bytecode restrictions and invokes the application's authorization callback.

Security review follow-up: `pnpm exec tsx --env-file=.env scripts/chain-wallet-concurrency.ts` passed against actual local PostgreSQL: 8 concurrent buyer-wallet requests and 8 concurrent NULL-principal seller-wallet requests each created exactly one wallet; foreign-principal tenant binding rejected. `ensureWallet` uses a tenant row lock and one database transaction to enforce this. The local fixture helper writes keys only to ignored mode-0600 `.local/chain.env`; it is explicitly local Anvil and never a public deployment manifest.

History indexing: `pnpm exec vitest run tests/history.test.ts packages/chain/policy.test.ts` passed 11 tests (8 history + 3 chain policy). History tests cover sealed-depth cutoff, replay idempotency, rebuild equality, checkpoint reorganization rollback and invalid-event exclusion, RPC failure without cursor movement, mid-scan reorganization rejection, sport filters and distinct service-timeout categories. Actual `syncChainHistory()` was executed twice against isolated `sportproof_chain_test` PostgreSQL and running local Anvil: first scan 155 blocks, next cursor 158; repeat scan 0; event-derived history reported 6 purchases, 6 delivered, 6 settled, 0 invalid. These are local-chain outcomes, not public Base Sepolia receipts; local history returns no BaseScan links.
